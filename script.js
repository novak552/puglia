
(function(){const data=window.TRIP_DATA||[];const map=L.map('map');L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'&copy; OpenStreetMap contributors'}).addTo(map);const colors=['#2563eb','#dc2626','#16a34a','#9333ea','#ea580c','#0891b2','#be123c','#4f46e5','#65a30d','#475569'];const layers=[];const bounds=[];let num=0;data.forEach((day,i)=>{const g=L.layerGroup().addTo(map);const pts=day.stops.map(s=>[s.lat,s.lon]);pts.forEach(p=>bounds.push(p));L.polyline(pts,{color:colors[i%colors.length],weight:4,opacity:.82}).addTo(g);day.stops.forEach((s,j)=>{if(i>0&&j===0&&s.name===data[i-1].stops[data[i-1].stops.length-1].name)return;num++;const ic=L.divIcon({className:'',html:`<div class="stop-icon${s.type==='swim'?' swim':''}">${num}</div>`,iconSize:[28,28],iconAnchor:[14,14]});L.marker([s.lat,s.lon],{icon:ic}).bindPopup(`<strong>${num}. ${s.name}</strong><br>Den ${day.day} • ${day.date}<br><a href="${day.mapsUrl}" target="_blank" rel="noopener">Otevřít den v Google Maps ↗</a>`).addTo(g)});layers.push(g)});if(bounds.length)map.fitBounds(L.latLngBounds(bounds),{padding:[25,25]});const filters=document.getElementById('dayFilters');const all=document.createElement('button');all.type='button';all.textContent='Vše';all.className='active';filters.appendChild(all);const bs=[];data.forEach((d,i)=>{const b=document.createElement('button');b.type='button';b.textContent=`Den ${d.day}`;b.className='active';filters.appendChild(b);bs.push(b);b.addEventListener('click',()=>{const l=layers[i];if(map.hasLayer(l)){map.removeLayer(l);b.classList.remove('active')}else{l.addTo(map);b.classList.add('active')}all.classList.toggle('active',bs.every(x=>x.classList.contains('active')))})});all.addEventListener('click',()=>{const show=!bs.every(x=>x.classList.contains('active'));layers.forEach((l,i)=>{if(show&&!map.hasLayer(l))l.addTo(map);if(!show&&map.hasLayer(l))map.removeLayer(l);bs[i].classList.toggle('active',show)});all.classList.toggle('active',show)})})();
